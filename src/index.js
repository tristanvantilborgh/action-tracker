import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import ActiepuntenTracker from './ActiepuntenTracker';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ActiepuntenTracker />
  </React.StrictMode>
);
